const state = {
    
    isLoading: false,
    fullPage: true,
    canCancel:false,
 
}

const getters = {
 
};

const actions = {
 
};

const mutations = { 
 
};

export default {
    namespaced: true,
    state, 
    getters,
    actions,
    mutations,
}
